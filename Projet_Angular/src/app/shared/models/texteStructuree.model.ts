export class TexteStructuree {
  arbre: string;
  estComplete: boolean;

  constructor(arbre: string, estComplete: boolean) {
        this.arbre = arbre;
        this.estComplete = estComplete;
  }
}
